server.port: 8081
spring:
  profiles: test
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:bookingrooms  #mem artinya adalah memori, berarti hanya dijalankan pada memori/RAM
    username: sa
    password:
  jpa.database-platform: org.hibernate.dialect.H2Dialect
  messages.basename: i18n/messages